define(function (require) {
    'use strict';

    var Scrollable = require('Scrollable/Scrollable');

    describe('scrollable', function () {

        it('should be an object', function () {
            expect(Scrollable instanceof Object).toBe(true);
        });

    });

});